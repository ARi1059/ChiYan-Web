# 复制为 apps/api/.dev.vars（已在 .gitignore），按需填入真实值。
# wrangler dev 启动时自动读这里；与 wrangler.toml 的 [vars] 块合并（这里优先级更高）。

# ─── ENV-aware（已在 wrangler.toml [vars] 给了 dev 默认值，这里覆盖用） ───
# ENV=dev
# ALLOWED_ORIGINS=["http://localhost:5173","http://localhost:5174","http://localhost:8787"]

# ─── secrets（生产环境用 wrangler secret put，dev 写这里） ───
# DATABASE_URL=postgres://...neon...
# UPSTASH_REDIS_REST_URL=https://...upstash.io
# UPSTASH_REDIS_REST_TOKEN=...
# JWT_SECRET=>=32 字节随机字符串（建议 openssl rand -base64 48）
# ENC_KEY_V1=base64 编码的 32 字节（openssl rand -base64 32）
# ENC_KEY_V2=base64 编码的 32 字节（轮换准备，可暂留空）
